Para cloner repos: git clone <<link_del_repos>>

Cuando hay que actualizar el repos: git pull

Cuando efectuaste modificaciones en el repos (abajo):
	git status
	git add .
	git commit -a -m "aqui agregas un comentario de lo que hiciste en comillas"
	git push

